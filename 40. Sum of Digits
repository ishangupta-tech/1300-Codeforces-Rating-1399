#include <bits/stdc++.h>
using namespace std;

int main() {
    string s;
    cin >> s;

    int ans = 0;

    while (s.size() > 1) {
        ans++;

        int sum = 0;

        for (char c : s)
            sum += c - '0';

        s = to_string(sum);
    }

    cout << ans;

    return 0;
}
