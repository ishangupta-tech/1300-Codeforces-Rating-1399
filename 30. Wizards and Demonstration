#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, x, y;
    cin >> n >> x >> y;

    int required = (n * y + 99) / 100;

    if (required <= x)
        cout << 0;
    else
        cout << required - x;

    return 0;
}
